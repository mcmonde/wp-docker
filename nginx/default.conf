# server {
#     listen 80;
#     server_name domain.com;
#
#     return 301 $host$request_uri;
#}
server {
    listen 80;
#     listen 443 ssl;
    server_name _;
    # === SSL CONFIGURATION ===
    # Point to the files inside the container path
#     ssl_certificate     /etc/nginx/certs/fullchain.crt;
#     ssl_certificate_key /etc/nginx/certs/private.key;
    # Recommended SSL Settings (Standard for paid certs)
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
    # ... (Rest of the config: root, logs, locations, etc) ...
    root /var/www/html;
    index index.php;
#    client_max_body_size 64M;
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass wordpress:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
    location = /xmlrpc.php {
    deny all;
    }
    location ~ /\.ht {
        deny all;
    }
    location /.well-known/acme-challenge/ {
        root /var/www/letsencrypt/;
        log_not_found off;
    }
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}

  # USE THIS IF YOU USE CERTBOT
# server {
#     listen 80;
#     server_name example.com; # CHANGE TO YOUR DOMAIN
#
#     location /.well-known/acme-challenge/ {
#         root /var/www/html;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# server {
#     listen 443 ssl;
#     server_name example.com; # CHANGE TO YOUR DOMAIN
#
#     # SSL Certificates (These path names depend on your domain)
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
#
#     root /var/www/html;
#     index index.php;
#
#     # Security & Logs
#     access_log /var/log/nginx/access.log;
#     error_log /var/log/nginx/error.log;
#
#     client_max_body_size 64M;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         try_files $uri =404;
#         fastcgi_split_path_info ^(.+\.php)(/.+)$;
#         fastcgi_pass wordpress:9000;
#         fastcgi_index index.php;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#     }
# }